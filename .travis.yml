sudo: false

language: python

env:
  - TOX_ENV=py26
  - TOX_ENV=py27
  - TOX_ENV=py32
  - TOX_ENV=py33
  - TOX_ENV=py34
  - TOX_ENV=pylint

install:
  - pip install tox
  - pip install pylint

script:
  - tox -e $TOX_ENV
